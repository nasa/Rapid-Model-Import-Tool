Planned Updates
=======================

Currently the following features are being worked on:

- Removal of screws and nuts by machine learning. 
- Additional file support for many CAD file types (.prt, .asm, .sldprt, .step).
- A config file where users can keep constant settings.
- The ability to export a file in a VR/AR setting.
